import './Setting.scss';

import SettingViewBoard from '../../../components/SettingViewComponents/SettingViewBoard/SettingViewBoard';

const Setting = () => {
  return (
    <div className='pg-setting-view'>
      <SettingViewBoard />
    </div>
  );
};

export default Setting;
